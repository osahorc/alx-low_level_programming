#include "main.h"

/**
 * _strlen - resets a pointer to 98
 *
 * @s: The value of the pointed
 *
 * Return: void
 */

int _strlen(char *s)

{
	int count;

	for (count = 0; s[count] != '\0'; count++)
	{
	}
	return(count);
}
