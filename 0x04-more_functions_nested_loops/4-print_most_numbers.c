#include "main.h"

/**
 * print_numbers - prints alphabets
 * 
 * Return: result of multiplication
 */
void print_numbers(void)
{
	int a
		for (a ='0'; a <= '9'; a++)
		{
			if (a != '2' && a != '4')

			_putchar((a % 10) + '0')
		}
	_putchar('\n');
}
