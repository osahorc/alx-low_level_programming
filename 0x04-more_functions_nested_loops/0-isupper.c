#include "main.h"

/**
 * _islower - prints alphabets
 * @c: charter to cehck
 * Return: 0 if lower
 * -1 if otherwise
 */
int _isrrr(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
