#include "main.h"

/**
 * print_sign - prints alphabets
 * @c: charter to cehck
 * Return: 0 if lower
 * -1 if otherwise
 */
int print_sign(int n)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
